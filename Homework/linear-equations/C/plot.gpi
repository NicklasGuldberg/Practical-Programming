set terminal png
set output 'plot.png'
set key top left
set xlabel 'N'
set ylabel 'time [s]'
f(x) = a1 * x ** a2 + a3
fit [0:400] f() 'out.txt' using 1:3 via a1, a2, a3

plot 'out.txt' using 1:3 with points title "User"
replot f(x) with lines title "Fit"
# replot 'out.txt' using 1:4 with lines title "System"
